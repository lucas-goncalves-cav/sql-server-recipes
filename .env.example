# SQL Server must accept this password: at least 8 characters with three of
# uppercase, lowercase, digits and symbols.
MSSQL_SA_PASSWORD=your_password_here

# Host port mapped to the container
SQLSERVER_PORT=1433
